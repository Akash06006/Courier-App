<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="com.courierdriver.common.BindingUtils" />

        <variable
            name="model"
            type="com.courierdriver.model.NotificationListModel.Body" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <androidx.cardview.widget.CardView
            android:id="@+id/cv_order_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/dp_10"
            android:layout_marginRight="@dimen/dp_10"
            android:layout_marginTop="@dimen/dp_5"
            android:layout_marginBottom="@dimen/dp_5"
            app:cardCornerRadius="@dimen/dp_20">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:padding="@dimen/dp_15">

                <TextView
                    android:id="@+id/tv_message"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentLeft="true"
                    android:layout_toLeftOf="@+id/img_notification"
                    android:paddingRight="@dimen/dp_5"
                    android:text="@{model.notificationTitle.toString()}"
                    android:textColor="@color/colorBlack"
                    android:textSize="@dimen/sp_14"
                    app:font="@{`regular`}" />

                <ImageView
                    android:id="@+id/img_notification"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_alignParentRight="true"
                    android:src="@drawable/ic_notification" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_below="@+id/tv_message"
                    android:paddingTop="@dimen/dp_5"
                    android:text="@{model.createdAt!=null?BindingUtils.covertTimeAgoToText(model.createdAt):@string/todo}"
                    android:textColor="@color/colorGrey"
                    android:textSize="@dimen/sp_12"
                    app:font="@{`regular`}" />

                <TextView
                    android:id="@+id/tv_reply"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_below="@+id/tv_message"
                    android:layout_alignParentRight="true"
                    android:layout_centerVertical="true"
                    android:layout_marginLeft="@dimen/dp_10"
                    android:layout_marginRight="@dimen/dp_10"
                    android:layout_marginTop="@dimen/dp_5"
                    android:background="@drawable/round_corners_home_tab"
                    android:backgroundTint="@color/colorPrimary"
                    android:paddingLeft="@dimen/dp_15"
                    android:paddingTop="@dimen/dp_5"
                    android:paddingRight="@dimen/dp_15"
                    android:paddingBottom="@dimen/dp_5"
                    android:text="@string/reply"
                    android:textColor="@color/colorWhite"
                    android:textSize="@dimen/sp_12"
                    app:font="@{`bold`}" />


                <!--
                                <TextView
                                    android:layout_width="wrap_content"
                                    android:layout_height="wrap_content"
                                    android:layout_alignParentRight="true"
                                    android:background="@drawable/round_corners_home_tab"
                                    android:backgroundTint="@color/colorPrimary"
                                    android:drawableLeft="@drawable/ic_cash"
                                    android:drawablePadding="@dimen/dp_2"
                                    android:drawableTint="@color/colorWhite"
                                    android:paddingLeft="@dimen/dp_10"
                                    android:paddingTop="@dimen/dp_5"
                                    android:paddingRight="@dimen/dp_10"
                                    android:paddingBottom="@dimen/dp_5"
                                    android:text="@{currencySign+model.totalOrderPrice}"
                                    android:textAlignment="center"
                                    android:textColor="@color/colorWhite"
                                    android:textSize="@dimen/sp_12"
                                    app:font="@{`bold`}" />
                -->

            </RelativeLayout>
        </androidx.cardview.widget.CardView>
    </LinearLayout>
</layout>